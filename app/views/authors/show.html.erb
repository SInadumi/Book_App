<p id="notice"><%= notice %></p>

<p>
  <strong>Gender:</strong>
  <%= @author.gender %>
</p>

<p>
  <strong>Name:</strong>
  <%= @author.name %>
</p>
<h2>
  「<%=@author.name%>」の著書
</h2>
<hr />
<ul>
  <%=@books.each do |book|%>
    <li><%=link_to(book.title, "/books/#{book.id}/edit")%></li>
  <%end%>
</ul>

<%= link_to 'Edit', edit_author_path(@author) %> |
<%= link_to 'Back', authors_path %>
